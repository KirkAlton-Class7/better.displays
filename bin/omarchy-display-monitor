#!/bin/bash
# omarchy:summary=Per-monitor resolution, scale, position, and transform
# omarchy:group=display
# omarchy:name=monitor
# omarchy:args=[list|set <output> [--mode WxH@Hz] [--scale N] [--pos XxY] [--transform N] [--dry-run]]
set -euo pipefail
exec python3 "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/monitor-settings.py" "$@"
